image : ghcr.io/astral-sh/uv:python3.11-bookworm-slim

stages:
  - lint
  - test
  - build
  - deploy

default:
  before_script:
    - uv sync --extra dev --locked

lint:
  stage: lint
  script:
    - ruff check
    - ruff format --check

test:
  stage: test
  script:
    - pytest

check :
  stage : build
  script : 
    - python -c "import app.api.main"

docker-build :
  stage: deploy
  image: docker:24
  services:
    - docker:24-dind
  before_script: [] 
  script:
    - docker build -t prediction-markets:latest .