/**
 * Config de la démo — change le client ici pour réadapter à un autre prospect.
 * (Le reste lit ces valeurs ; rien n'est codé en dur ailleurs.)
 */
export const CONFIG = {
  product: 'Tempo',
  client: 'Unique Heritage Media',
  clientShort: 'UHM',
  accent: '#7c5cff',
} as const
